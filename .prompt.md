<!-- PROMPT CONFIG
Language: dotnet
Extension: dachan
Modules: mongodb
Generated: 2026-02-13 16:30:00
-->

---

# AI 協作通用原則

## 原則優先順序
當原則衝突時，依此順序判斷：**可讀性 (文章式描述) > KISS > DRY > 抽象化**

---

## Clean Code 與高品質開發原則

### 1. 結構 (Structure)
*   **目錄分層 (Clean Architecture)**：
    *   `/src/Dachan.Wms/Controllers`：API 進入點，僅處理 Request/Response，不含商業邏輯。
    *   `/src/Dachan.Wms/Interfaces`：業務邏輯介面層 (Service Interfaces)。
    *   `/src/Dachan.Wms/Services`：業務邏輯實作層 (Application Layer)，**必須使用 partial class** 以支援高效能日誌。
    *   `/src/Dachan.Wms/Domain/Entities`：核心領域實體 (Domain Entities)。
    *   `/src/Dachan.Wms/Domain/Events`：領域事件定義 (Domain Events)。
    *   `/src/Dachan.Wms/Domain/IRepositories`：倉儲介面定義 (Repository Interfaces)。
    *   `/src/Dachan.Wms/Event`：事件處理器，包含領域事件 (DomainEventHandlers) 與外部訊息 (RabbitMqEventHandle)。
    *   `/src/Dachan.Wms/Infrastructure/Factories`：靜態工廠，負責 DTO 與 Entity 之間的物件映射。
*   **職責分離 (Responsibility)**：
    *   **Shared DTO**：僅負責資料結構定義，**禁止**包含 UI 顯示邏輯（如轉中文、格式化）。
    *   **Client Extensions**：所有顯示轉換（Enum 轉中文等）必須定義在前端的擴充方法中（如 `DisplayExtensions.cs`）。
*   **方法長度**：建議 50 行內，特殊情況可彈性處理。
*   **早期返回 (Early Return)**：先處理錯誤/邊界條件，減少嵌套。
*   **避免深層嵌套**：最多 2-3 層，超過應抽取方法。
*   **單一職責 (SRP)**：一個類別/方法只做一件事。
*   **成員間距**：不同方法或成員變數之間，**固定僅保留一行空行**，禁止多餘的連續空行。

### 2. 命名 (Naming)
*   **禁止縮寫**：除非是公認縮寫（如 HTTP, JSON, DTO, ID）。
*   **描述性 Lambda 參數**：**禁止**使用無意義的 `x` 或 `i`。必須使用具備業務意圖的名稱，例如 `warehouse => warehouse.Code`。
*   **有意義命名**：禁止 `temp`、`data`、`info`、`obj` 等模糊名稱。
*   **布林命名**：使用 `is`, `has`, `can`, `should` 開頭。
*   **方法命名**：動詞開頭（Get, Create, Update, Delete, Validate, Apply）。
*   **Enum 成員**：必須使用 **PascalCase**，禁止使用底線（例如 `HalfH` 而非 `Half_H`）。

### 3. 語意命名規範表
| 名稱 | 用途 | 格式 |
|------|------|------|
| `Id` | 機器識別（UUID/GUID/ObjectId） | 人類不可讀 |
| `Code` | 人類識別 | 英數、日期、簡單符號混合 |
| `Type` | 分類標記 | 必須用 Enum，傳輸/儲存用 string |

### 4. 設計與防禦 (Design & Defense)
*   **DRY**：重複邏輯超過 **3 次**才抽取。
*   **KISS**：優先選擇簡單方案。
*   **依賴注入**：透過建構函式注入，禁止在方法內 `new` 依賴。
*   **避免魔術數字**：必須用 const 或 enum 命名。
*   **Guard Clauses**：方法開頭驗證參數有效性。
*   **Null 安全**：啟用 Nullable Reference Types，明確處理 null。

---

## 集合與封裝 (Collections & Encapsulation)

*   **集合型別 (CA1002)**：公用屬性應使用 `ICollection<T>`、`IEnumerable<T>` 或 `IReadOnlyCollection<T>`，禁止直接公開 `List<T>`。
*   **唯讀化 (CA2227)**：集合屬性應設為唯讀或使用 **`init;`**，防止外部代碼替換整個集合實例。
*   **範例**：
    ```csharp
    public ICollection<OrderDetail> Details { get; init; } = new List<OrderDetail>();
    ```

---

## 高效能日誌 (Logging - CA1848)

*   **Source Generators**：優先使用 **`[LoggerMessage]`** 屬性定義日誌方法，以提升效能並減少裝箱損耗。
*   **檔案拆分**：日誌定義應放在與主程式隔離的 **`*.Logging.cs`** 檔案中。
*   **泛型處理**：在泛型類別中若無法使用 Source Generator，則應改用 `LoggerMessage.Define`。

---

## 異常處理 (Exception Handling - CA1031)

*   **自定義錯誤**：僅在需要「特定業務錯誤訊息」或「自定義錯誤代碼」時才撰寫捕捉邏輯。
*   **集中定義**：WMS 專屬錯誤必須定義在 `WmsError` 靜態類別中，比照 `ApiClientError` 風格。
*   **全域處理**：若不需要自定義錯誤，則不要撰寫 `try-catch`，讓異常自然向上拋出給全域 Middleware 處理。
*   **禁止行為**：禁止在 Service 或 Controller 中撰寫僅為了記錄 Log 而重新 throw 的冗餘代碼。

---

## 查詢與過濾 (Fluent API 模式)

*   **兩行式初始化**：建立查詢選項時，將實例化與分頁配置分開。
    ```csharp
    var queryOptions = new QueryOptions<Warehouse>();
    queryOptions.ApplyPaging(queryDto);
    ```
*   **描述性過濾**：使用 `QueryOptionsExtensions` 提供的 `Add...IfProvided` 系列方法，並配合「具名參數」提升識別度。
    ```csharp
    queryOptions.AddStartsWithIfProvided(value: queryDto.Code, targetField: warehouse => warehouse.Code);
    ```

---

## 時間處理
*   **統一時區**：所有時間進入服務後必須轉換為 **UTC+0**。
*   **儲存格式**：資料庫儲存使用 UTC+0。
*   **轉換時機**：僅在 API 輸出給前端時，依需求轉換為當地時區。

---

## .NET 開發規範

### 1. 格式化
*   **命名空間 (Namespace)**：
    *   統一使用 **File-scoped namespace** (例如：`namespace Dachan.Wms.Services;`)，不帶大括號。
    *   **間距要求**：`namespace` 宣告與上方的 `using` 指令群之間，以及與下方的程式碼（類別/介面定義）之間，**固定僅保留一行空行**。
*   **縮排**：使用 **4 個空格**。
*   **Using 指令**：放在命名空間外部，保持簡潔，禁止帶入一長串 Namespace 路徑。
*   **非同步**：IO 操作必須使用 Async/Await。

### 2. 環境變數管理
*   **必須**：使用 `EnumEnvironmentVariable` + 擴充方法（如 `.GetRequiredString()`）。
*   **禁止**：禁止在程式碼中直接呼叫 `Environment.GetEnvironmentVariable()`。

### 3. 物件映射
*   **禁止 AutoMapper**：不使用自動映射套件。
*   **手動映射**：簡單轉換寫在使用處；重複超過 3 次則抽取至 `Infrastructure/Factories` 下的工廠類別。

### 4. 充血模型 (Rich Domain Model)
*   **適用範圍**：僅限處理物件本身參數的內部邏輯（如狀態驗證、欄位計算）。
*   **禁止**：不得牽扯外部資料、外部服務、或跨物件邏輯。

### 5. Enum 規範
*   **定義**：分類欄位必須使用 Enum。
*   **傳輸/儲存**：API Request/Response 與資料庫儲存一律使用 **string**。
*   **禁止**：禁止用 int 表示 Enum 值。

---

## 大成專案與 MongoDB 規範

### 1. 私有套件引用
*   **核心工具**：`Dachan.CommonUtils` (環境變數、擴充方法)。
*   **Web 框架**：`Dachan.CommonUtils.Web` (ApiResponseMiddleware, DachanHttpClientProxy)。
*   **事件驅動**：`Dachan.Common.EventDriven` (處理異步領域事件、RabbitMQ 訊息整合)。
*   **資料存取**：`Dachan.MongoRepository` (自動掃描 `[UseRepository]`)。

### 2. API 設計
*   **回傳型別**：Controller 直接回傳 POCO 或 `Task<T>`，**嚴禁手動封裝 Response**（由 Middleware 統一處理）。
*   **異常處理**：業務錯誤拋出 `DachanServiceException`，搭配 `WmsError` 或 `DachanCommonError`。

### 3. Entity 規範
*   **繼承**：必須繼承 `AuditableEntityBase` 或 `SoftDeletableEntity`。
*   **存放位置**：必須放在 `/src/Dachan.Wms/Domain/Entities` 下。
*   **標註**：必須包含 `[BsonCollection]` 與 `[UseRepository]`。

### 4. MongoDB 實務
*   **命名**：Collection 命名使用 **snake_case**（如 `outbound_orders`）。
*   **查詢優化**：優先使用投影 (Projection)，批次操作使用 `BulkWrite`。

---

## 前端 (Blazor) 開發規範

*   **元件庫**：UI 開發優先使用 **MudBlazor** 組件。
*   **魔術字串處理**：進行 Enum 類型比對或賦值時，應使用 **`nameof(EnumType.Value).ToLowerInvariant()`**。
*   **選單管理**：導覽選單必須透過 `SideBarHelper` 與 `SideBarNode` 邏輯產生。

---

## 文件化 (Documentation)
*   **XML Documentation**：所有 `public` 方法與類別必須包含 **繁體中文** 說明。
*   **OpenAPI**：配合 Swagger 提供清晰的 API 文件。
